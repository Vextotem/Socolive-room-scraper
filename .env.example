PORT=8000
SITE_ORIGIN=https://m.socolivewl.net
DATA_ORIGIN=https://json.vnres.co
REQUEST_TIMEOUT_MS=20000

# Exact allowed media hostnames, comma-separated
PROXY_ALLOWED_HOSTS=pull.niues.live,pull06.scstream.net
# Set a stable random secret in production (see README).
# SESSION_SECRET=
# Legacy fallback: PROXY_SECRET (used only when SESSION_SECRET is unset).
